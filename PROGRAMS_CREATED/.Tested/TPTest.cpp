# include <iostream>

using std::cout;
using std::cin;

int main(){

int playerCount = 12;

cout << ((playerCount >= 10) ? "Player gap reached starting match..." : "Not enough people in the match\n");

return 0;

}